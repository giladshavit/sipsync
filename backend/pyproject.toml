[project]
name = "sipsync-backend"
version = "0.1.0"
description = "SipSync real-time party game — FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.138.0",
    "pydantic>=2.13.4",
    "redis>=8.0.0",
    "uvicorn[standard]>=0.49.0",
]
